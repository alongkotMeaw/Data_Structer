#include <stdio.h>
#include "sumTime.h" // Include the header file to use the functions

int main(int argc, char const *argv[])
{
    printf("%d\n", start()); // Call the start function
    printf("%d\n", end());   // Call the end function

    return 0;
}
